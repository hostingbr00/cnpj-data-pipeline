# CNPJ Data Pipeline Configuration

# Database (PostgreSQL via Docker on port 5436)
DATABASE_URL=postgres://postgres:postgres@localhost:5436/cnpj

# Processing
BATCH_SIZE=500000
TEMP_DIR=./temp

# Downloads (parallel)
DOWNLOAD_WORKERS=4

# Network
RETRY_ATTEMPTS=3
RETRY_DELAY=5
CONNECT_TIMEOUT=30
READ_TIMEOUT=300

# Cleanup
KEEP_DOWNLOADED_FILES=false
